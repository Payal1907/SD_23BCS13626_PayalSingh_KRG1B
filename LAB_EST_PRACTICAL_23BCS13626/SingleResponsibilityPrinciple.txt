//SINGLE RESPONSIBILITY PRINCIPLE

//STATES THAT EVERY CLASS MUST PERFORM A SINGLE FUNCTIONALITY.
//IMPLEMENTATION OF MULTIPLE-FUNCTIONALITIES IN A SINGLE
//CLASS CAN CAUSE TIGHT COUPLING

CLASS BANK_SERVICE{
	PUBLIC DEPOSIT(AMOUNT, ACCOUNT_NO)
	{
		RETURN DEPOSIT_AMOUNT;
	}
	PUBLIC WITHDRAW(AMOUNT, ACCOUNT_NO)
	{
		RETURN WITHDRAWAL_AMOUNT;
	}
	PUBLIC PRINT_PASSBOOK(ACCOUNT_NO) {
		//LOGIC TO UPDATE PASSBOOK INFO
	}
	PUBLIC GET_LOAN_INFO(LOAN_TYPE) {
		IF (LOAN_TYPE = "HOMELOAN") {
			//LOGIC
		}
		IF (LOAN_TYPE = "PERSONAL") {
			//LOGIC
		}
		IF (LOAN_TYPE = "CAR"){
			//LOGIC
		}
	}
	PUBLIC SEND_OTP(METHODS) {
		IF (METHOD == "EMAIL") {
			//WRITE EMAIL API TO SEND THE OTP
		}
	}
}

//IF WE WANT TO HAVE A FUNCTIONALITY IN WHICH YOU WANT TO SEND THE OTP ON MOBILE
//SEPERATE CLASSES
CLASS PRINTER_SERVICE{
	PUBLIC PRINT_PASSBOOK(ACCOUNT_NO) {
		//LOGIC TO UPDATE PASSBOOK INFO
	}
}

CLASS LOAN_SERVICE{
	PUBLIC VOID GET_LOAN_INFO(STRING LOAN_TYPE) {
		IF (LOAN_TYPE = "HOMELOAN") {
			//DO SOME LOGIC
		}
		IF (LOAN_TYPE = "PERSONAL") {
			//DO SOME LOGIC
		}
		IF (LOAN_TYPE = "GOLD") {
			//DO SOME LOGIC
		}
	}
}

CLASS NOTIFICATION_SERVICE{
	PUBLIC VOID SEND_OTP(STRING METHODS) {
		IF (METHOD == "EMAIL") {
			//WRITE EMAIL API TO SEND THE OTP
		}
		IF (METHOD == "MOBILE") {
			//WRITE MOBILE API TO SEND THE OTP - TULLIO MOBILE API 
		}
	}
}